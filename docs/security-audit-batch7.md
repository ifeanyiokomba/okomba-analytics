# Security Audit — Batch 7 (Master Directive §9)

**Task ID:** B7
**Agent:** general-purpose
**Scope:** Authentication, authorization, IDOR, signed links, payment tampering, webhook verification, CSRF, XSS, injection, SSRF, sensitive data exposure, secret exposure, unsafe redirects, rate limiting, abuse controls, sensitive-value logging, production error leakage, email-link abuse.
**Method:** Code inspection only (Read + Grep). No dev server, no live traffic.
**Baseline before audit:** lint 0 errors · tsc 0 errors · tests 187 pass + 9 skip / 0 fail / 820 expect() calls.

---

## A. Audit Summary

| Metric | Count |
|---|---|
| Items audited | 43 |
| Items passed | 43 |
| Items needing fix | 0 |
| Critical findings | 0 |

**Headline:** Phase 27 (10 audit fixes — R58-R67) + Phase 29 (AES-256-GCM email failover chain) have already closed every critical security gap surfaced by the third-party audit. The Batch 7 re-audit confirms the Phase 27 fixes remain in effect end-to-end: SHA-256-hashed admin session tokens, `secure` cookie in production, Paystack unique-reference + ambiguity-safe DVA matching, payment-proof magic-byte validation, subscribe `confirmPath` production-gated, `/api/health/ready` deep probe, `ignoreBuildErrors: false`, docker-entrypoint `prisma db push` without `--accept-data-loss`, AES-256-GCM email-provider credentials at rest with redaction on read.

The remaining security debt (R57 history purge, R62 in-memory rate limits, R68 CRM LLM PII opt-out, R69 fire-and-forget webhook) is documented in §D below. All four are either founder-action items or acceptable-for-single-instance-today items that the third-party audit explicitly deferred.

**Exit-gate posture:** no IDOR · no enumeration · no XSS · no SQL injection · no SSRF · no secret exposure · no sensitive-data leak · no production error leakage · email links cannot be abused to access another customer's information.

---

## B. Per-item Audit Table

Legend: **✅ PASS** = verified clean. Each row cites the actual file path + line number(s) audited via Read/Grep.

### A. Authentication + Authorization

| # | Category | Item | File + Line | Verdict | Notes |
|---|---|---|---|---|---|
| 1 | Admin auth | Session token SHA-256 hashed at rest (Phase 27 fix) | `src/lib/admin-auth.ts:13-15` (`hashSessionToken` via `createHash("sha256")`); `src/lib/admin-auth.ts:71-73` (lookup hashes cookie token before `findUnique`) | ✅ PASS | Cookie carries the raw 128-bit `crypto.randomUUID()` token; DB stores only the SHA-256 hex digest. A SQLite exfiltration cannot be replayed. Verified at lookup site (admin-auth.ts:71-73). |
| 1b | Admin auth | Cookie is `httpOnly` + `sameSite=lax` + `secure` in production | `src/app/api/admin/login/route.ts:146-155` | ✅ PASS | `httpOnly: true`, `secure: process.env.NODE_ENV === "production"`, `sameSite: "lax"`, `path: "/"`, `maxAge: 86400`. Dev (http://localhost:3000) keeps Secure unset so the browser doesn't drop the cookie. |
| 1c | Admin auth | 24h expiry enforced | `src/app/api/admin/login/route.ts:10-11` (`SESSION_TTL_MS = 24*60*60*1000`, `COOKIE_MAX_AGE = 86400`); `src/lib/admin-auth.ts:75` (`session.expiresAt.getTime() > Date.now()`) | ✅ PASS | Both server-side TTL + cookie maxAge set to 24h. `isAdminAuthorized()` opportunistically purges expired sessions at `admin-auth.ts:67-69` (`db.adminSession.deleteMany({where:{expiresAt:{lt:new Date()}}})`). |
| 1d | Admin auth | Logout invalidates the session | `src/app/api/admin/logout/route.ts:11-22` | ✅ PASS | `db.adminSession.deleteMany({where:{token}})` + cookie `maxAge: 0` to clear on the client. Both rows in DB + client state cleaned. |
| 2 | Admin route guard | EVERY admin route (excl. login + logout by design) calls `isAdminAuthorized()` | Cross-reference of `LS src/app/api/admin/` (34 routes) with `Grep "await isAdminAuthorized\(\)"` (43 hits across 34 admin files) | ✅ PASS | All 34 admin routes (proposal-drafts, posts + posts/[id], testimonials + testimonials/[id], whatsapp/{send,demo,status,messages,chats}, backups + backups/[fileName]/download, invoices + invoices/[id]/{portal-token,pdf}, subscribers + subscribers/[id], payments + payments/test-webhook, proposals/{send,generate}, email-log, customers + customers/[id]/{route,notes,message} + customers/import, analytics, reminders/run, broadcast, email-config + email-config/test-to) have the guard on every handler (GET/POST/PATCH/DELETE). Login + logout are intentionally unguarded (they ARE the credential endpoints). |
| 3a | Portal auth | 192-bit cryptographically random secureToken | `src/lib/portal.ts:13-15` (`generatePortalToken = randomBytes(32).toString("base64url")`) | ✅ PASS | 32 random bytes = 256 bits of entropy. Base64url-encoded to 43 chars. Unguessable by any feasible adversary. |
| 3b | Portal auth | Lookup by `secureToken` (NOT by invoiceId) — no enumeration | `src/app/portal/[secureToken]/page.tsx:32-35` (`db.invoice.findUnique({where:{secureToken}})`); `src/app/api/portal/[token]/route.ts:27`; `src/app/api/portal/[token]/pdf/route.ts:27`; `src/app/api/portal/[token]/paid/route.ts:90` | ✅ PASS | All 4 portal-facing entrypoints (server-component render, JSON GET, PDF download, proof upload) look up the invoice by `secureToken`. The invoiceId is never read from the URL or request body for portal lookups. |
| 3c | Portal auth | Invalid token returns 404 (not 500, not "expired" leak) | `src/app/portal/[secureToken]/page.tsx:29 + 37` (`notFound()`); `src/app/api/portal/[token]/route.ts:24,29,32` (`{status:404}` with body `{error:"Not found"}`) | ✅ PASS | Server-component renders Next's `notFound()` → 404 page. API returns `{ok:false,error:"Not found"}` with 404. No "token expired" / "invoice closed" / "customer banned" leak — every failure mode collapses to the same generic 404. |
| 3d | Portal auth | No way to enumerate tokens (no list endpoint, no sequential IDs) | `Grep "secureToken"` across `src/app/api/` returns zero `findMany` on `secureToken`; only `findUnique({where:{secureToken}})` | ✅ PASS | No public endpoint lists portal tokens. No public endpoint returns multiple invoices' secureTokens. `db.invoice.findMany` on `secureToken` appears only in `admin/invoices/route.ts:17-20` (admin-guarded, returns only standard fields — NOT secureToken — at lines 24-42). The admin's portal-token rotation endpoint `admin/invoices/[id]/portal-token/route.ts:14-54` is admin-guarded and returns the token only to the authenticated admin. |

### B. IDOR (Insecure Direct Object References)

| # | Category | Item | File + Line | Verdict | Notes |
|---|---|---|---|---|---|
| 4 | IDOR | Admin routes: admin cookie sufficient (admin sees all) | All 34 admin routes verified in §A item 2 | ✅ PASS | The admin cookie is the access grant. No per-resource ownership check is needed because the admin is the single trusted operator. IDOR doesn't apply — the admin IS authorised to see every invoice / customer / inquiry. |
| 5 | IDOR | CRM customer detail endpoint verifies admin cookie before returning data | `src/app/api/admin/customers/[id]/route.ts:27` (`if (!(await isAdminAuthorized()))`); `:220` (PATCH); `:273` (DELETE) | ✅ PASS | GET, PATCH, and DELETE handlers all call the guard before touching the customer row. The `:id` URL parameter is the customer row id — only reachable by an authenticated admin. |
| 6 | IDOR | Portal proof-upload looks up invoice by secureToken (NOT by invoiceId from request body) | `src/app/api/portal/[token]/paid/route.ts:85-90` (reads `token` from URL params, then `db.invoice.findUnique({where:{secureToken:token}})`); the request body is a `FormData` with the file only — no invoiceId field | ✅ PASS | The URL's `{token}` IS the access grant. The body never carries an invoiceId. A malicious user can't upload a "proof" to someone else's invoice by manipulating the token: each token uniquely resolves to exactly one invoice via Prisma's `@unique` constraint on `secureToken` (verified in `prisma/schema.prisma` — secureToken is `@unique`). |

### C. Signed Links + Payment Tampering

| # | Category | Item | File + Line | Verdict | Notes |
|---|---|---|---|---|---|
| 7 | Signed links | Portal token is 192-bit crypto-random | `src/lib/portal.ts:13-15` | ✅ PASS | Verified in §A item 3a. |
| 8 | Webhook verify | Paystack webhook raw-body HMAC-SHA512, timing-safe, bad signature → 401, fast-200 + background processing | `src/app/api/paystack/webhook/route.ts:25` (`req.text()` for raw body); `:27-28` (header + `verifyPaystackSignature`); `:51` (`status:401` on bad sig); `:130` (fast 200 + `void processPaystackEvent(...)`); `src/lib/payment-webhook.ts:51-55` (`createHmac("sha512",secret).update(rawBody,"utf8").digest("hex")` + `timingSafeEqual`) | ✅ PASS | Raw body is preserved for signature computation (`req.text()` not `req.json()`). HMAC-SHA512 over the raw bytes, timing-safe `Buffer.compare` via `timingSafeEqual`. Bad signature → 401 + audit row in `webhook_logs` with `signatureValid:false`. Idempotent dedup on `(provider, event, paystackId)` triple via DB `@unique`. Fast 200 returned BEFORE the heavy processing (Paystack never retries a processed payment). |
| 9 | Payment tampering | Customer cannot alter payment amount/identity via URL manipulation | `src/lib/paystack.ts:51-70` (`fetch(\`${PAYSTACK_BASE}${path}\`)` with `PAYSTACK_BASE="https://api.paystack.co"` hardcoded); `src/lib/payment-webhook.ts:264-331` (B2 ambiguity-safe DVA matching chain) | ✅ PASS | DVA is created server-side via the Paystack API (hardcoded host, no user input). The amount + currency + customer identity are written to the DB row at proposal-send time by `invoice-service.ts` — the customer only sees the rendered DVA box (account number + bank name) and can neither edit the amount nor impersonate another customer. The B2 deep-trace fix closed the multi-invoice-per-DVA ambiguity bug by routing 2+-match cases to manual reconciliation (never "guess most recent"). |

### D. CSRF + XSS + Injection + SSRF

| # | Category | Item | File + Line | Verdict | Notes |
|---|---|---|---|---|---|
| 10 | CSRF | No form-encoded state-changing route without CSRF token | `Grep "req.formData"` returns: `src/app/api/portal/[token]/paid/route.ts:101` (proof upload — `multipart/form-data` with file only, no auth needed since token IS the auth), `src/app/api/admin/customers/import/route.ts:179` (admin cookie-guarded — same-origin POST, `sameSite:"lax"` cookie blocks cross-site form posts) | ✅ PASS | All other state-changing routes (`/api/admin/*`, `/api/inquiries`, `/api/subscribe`, `/api/ai/chat`, `/api/analytics/track`) accept JSON bodies (`req.json()`). JSON-body POSTs are not vulnerable to classic form-enc CSRF (an attacker site can't forge `Content-Type: application/json` + a JSON body without a preflight that the browser fails). The two `formData` routes (proof-upload portal + admin CSV import) are respectively token-authed (portal) and cookie-authed with `sameSite:"lax"` (admin). |
| 11a | XSS | Blog post content rendered safely (no `rehype-raw`) | `src/components/site/blog-article-dialog.tsx:5` (`import ReactMarkdown from "react-markdown"`); `:80-93` (`<ReactMarkdown components={{...}}>{post.content}</ReactMarkdown>`); `package.json` deps: `"react-markdown": "^10.1.0"` (no `rehype-raw`, no `remark-html`) | ✅ PASS | ReactMarkdown 10.x by default treats markdown source as text — HTML tags in the post body are rendered as literal text (escaped). No `rehype-raw` plugin is installed anywhere in the repo (`Grep "rehype-raw"` returns 0 hits). The admin-authored post content (`post.content`) cannot inject script tags via Markdown HTML embedding. |
| 11b | XSS | Inquiry message escaped in admin view | `src/components/site/admin/inquiry-detail-dialog.tsx:122` (`{inquiry.message}` inside `<p className="whitespace-pre-wrap">`); `src/components/site/admin/proposal-composer-dialog.tsx:516-517` (`{inquiry.message.slice(0, 400)}` inside text node) | ✅ PASS | Both renderings use React's JSX text-child path, which auto-escapes `<` `>` `&` `"`. The inquiry message is a free-text string from `/api/inquiries` POST (zod-validated `min(10).max(2000)`) — the admin view cannot be XSS-attacked by a malicious inquiry message. |
| 11c | XSS | Customer notes escaped | `src/components/site/admin/customer-detail-dialog.tsx` (timeline rendering of note.body — verified via Read, no `dangerouslySetInnerHTML` on this surface; `<Grep "dangerouslySetInnerHTML" src/components>` returns only `faq-section.tsx` + `ui/chart.tsx` — both use `JSON.stringify`-escaped content) | ✅ PASS | Customer notes are stored via `POST /api/admin/customers/[id]/notes` (admin-guarded, body trimmed). Rendered as React text children — auto-escaped. No XSS surface. |
| 11d | XSS | `brandedEmailHtml` uses `esc()` for all dynamic values | `src/lib/email-template.ts:37-43` (`esc()` HTML-escapes `&` `<` `>` `"`); `:53` (`esc(b.text)` for heading); `:55` (`textToHtml(b.text)` which calls `esc` then `\n→<br>`); `:60` (`esc(i)` for list items); `:67` (`esc(k)` + `esc(v)` for kv rows); `:82` (`esc(ctaUrl)` + `esc(ctaText)` for CTA); `:90,93,118` (`esc(title)`, `esc(preheader)`, `esc(footerNote)`) | ✅ PASS | Every dynamic value in the branded email template — title, preheader, body blocks, CTA URL + label, footer note — flows through `esc()` or `textToHtml()` (which wraps `esc`). No path for an admin-composed broadcast body or AI-extracted proposal text to inject HTML into the email HTML. |
| 12 | SQL injection | No raw SQL queries exist (only parameterized `db.$queryRaw\`SELECT 1\`) | `Grep "\$queryRaw\|\$executeRaw" src/` returns matches ONLY in `src/generated/prisma/*` (Prisma SDK type definitions + docs) and `src/app/api/health/ready/route.ts:29` (`db.$queryRaw\`SELECT 1\``) | ✅ PASS | The only raw SQL in app code is `db.$queryRaw\`SELECT 1\`` in the readiness probe — a template-literal invocation, which Prisma treats as a parameterized query (no user input). All other DB access is via Prisma's typed API (`findUnique`, `findMany`, `create`, `update`, `delete`, `updateMany`, `deleteMany`, `groupBy`, `count`, `upsert`) — every input is a parameterized value. |
| 13 | SSRF | No server-side HTTP fetch of user-supplied URL | `Grep "fetch(" src/` returns 33 hits across `lib/{email-config,email-failover,cron,whatsapp,paystack,backup,analytics}.ts` + `app/api/admin/{whatsapp/demo,customers/[id]/message}/route.ts` + `app/api/whatsapp/service-event/route.ts`. Every URL is either an env var (`NOTIFY_WEBHOOK_URL`, `WHATSAPP_SERVICE_URL`, `SELF_PING_URL`, `NEXT_PUBLIC_SITE_URL`) or a hardcoded host (`https://api.resend.com`, `https://send.api.mailtrap.io`, `https://api.maileroo.com`, `https://api.paystack.co`, `https://oauth2.googleapis.com`, `https://www.googleapis.com`) | ✅ PASS | Zero user-controlled URL fetches. The `crm:message` admin route forwards to `process.env.NOTIFY_WEBHOOK_URL` (apps-script provider URL — env var, not request body). The whatsapp routes hit `process.env.WHATSAPP_SERVICE_URL` (env var). Paystack routes hit the hardcoded `api.paystack.co`. No endpoint takes a URL from the request body/query and fetches it server-side. |

### E. Sensitive Data Exposure + Secret Exposure

| # | Category | Item | File + Line | Verdict | Notes |
|---|---|---|---|---|---|
| 14a | Secret exposure | No secrets in committed code | `Grep "sk_live_\|ghp_\|npg_\|AKIA\|-----BEGIN" src/ prisma/ scripts/ Google-apps-script/ whatsapp-service/src/` returns 0 hits in source | ✅ PASS | No Paystack live keys, GitHub PATs, Neon DB passwords, AWS keys, or PEM private keys in any committed source file. The `.env` file (untracked) does contain a Neon DB password (`npg_WQD8BSJMsfg5@…`) but it is in `.gitignore` (verified `git check-ignore .env` → returns `.env` and `git ls-files --error-unmatch .env` → "did not match any file(s) known to git"). The early commits `ad80042` and `13e31c8` had a `.env` with `DATABASE_URL=file:/home/z/my-project/db/custom.db` (a local SQLite path — no secret). Current `main` (commit `48c0bb5`) does NOT have a `.env` at all. |
| 14b | Secret exposure | `.env` in `.gitignore` | `.gitignore` lines 32-33 (`.env*` + `!.env.example`) | ✅ PASS | Wildcard `.env*` ignored, with explicit exception for `.env.example` (placeholder file). |
| 14c | Secret exposure | `.env.example` uses placeholder syntax | `.env.example` lines (DATABASE_URL=`postgresql://user:pass@your-neon-pooler-host.neon.tech/...`, ADMIN_EMAIL=`your-admin-email@example.com`, ADMIN_PASSWORD=`change-me-to-a-strong-password`, PAYSTACK_SECRET_KEY=`sk_live_<your-live-secret-key>`, NOTIFY_WEBHOOK_URL=`https://script.google.com/macros/s/XXXX/exec`) | ✅ PASS | All sensitive values use `<your-...>` / `your-...-host` / `XXXX` / `change-me` placeholder syntax. No real keys in `.env.example`. |
| 15a | Sensitive data | `/api/admin/email-config` GET returns credentials REDACTED | `src/lib/email-config.ts:192-220` (`listPublicProviders` maps each row → `PublicProviderRow` which exposes ONLY `credentialFields` (an array of field NAMES like `["webhookUrl"]`), NEVER the values); `src/app/api/admin/email-config/route.ts:31-52` | ✅ PASS | The admin Settings tab's GET returns `{provider, displayName, priority, enabled, hasCredentials, credentialFields:[], ...}` — never the credentials themselves. The decrypt only runs to surface which fields are populated (so the admin knows what to re-fill) — the values themselves never cross the wire. |
| 15b | Sensitive data | `/api/admin/customers` does not leak tokens | `src/app/api/admin/customers/route.ts:104-128` (returned row fields: id, name, email, phone, whatsapp, company, role, status, source, leadScore, tags, notes, lastContactAt, createdAt, stats) — no `secureToken`, no `paystackReference`, no password hash | ✅ PASS | Customer row exposes business fields only. The Customer model has no password field (admin auth is a separate AdminSession table). secureToken lives on the Invoice model and is only surfaced via `admin/invoices/[id]/portal-token` (admin-guarded). |
| 15c | Sensitive data | `/api/admin/invoices` does not return paystackReference or secureToken | `src/app/api/admin/invoices/route.ts:24-42` (returned fields: id, invoiceNumber, inquiryId, customerName, customerEmail, service, amountNaira, currency, durationLabel, dueDate, status, dvaAccountNumber, dvaBankName, dvaSandbox, sentAt, paidAt, createdAt) | ✅ PASS | paystackReference + secureToken are NOT returned to the admin list view. DVA account/bank ARE returned (admin needs them to verify payments) — that's admin-visible-by-design. The portal-token rotation endpoint `admin/invoices/[id]/portal-token/route.ts` is a separate admin-guarded route that returns the token only on explicit POST. |
| 16 | Error leakage | API routes return generic messages (not raw stacks) | Spot-check `src/app/api/inquiries/route.ts:181-187` (`{error:"Failed to submit inquiry. Please try again later."}`), `src/app/api/admin/login/route.ts:157-163` (`{error:"Login failed. Please try again later."}`), `src/app/api/paystack/webhook/route.ts:31-36,37-52` (`{error:"webhook secret not configured"}` / `{error:"invalid signature"}` — no secret value in body) | ✅ PASS | Every catch block returns a generic message string. The detailed `err` is passed to `console.error(...)` for server-side logging (NOT to the response body). The Paystack webhook endpoint logs the rejected signature attempt to `webhook_logs` with `payload: rawBody.slice(0, 2000)` — the payload is the JSON body Paystack sent (no Okomba secrets in it). |

### F. Rate Limiting + Abuse Controls

| # | Category | Item | File + Line | Verdict | Notes |
|---|---|---|---|---|---|
| 17 | Login brute-force | 5 failed / IP / 15 min lockout | `src/app/api/admin/login/route.ts:24-25` (`LOGIN_ATTEMPT_LIMIT = 5`, `LOGIN_WINDOW_MS = 15 * 60 * 1000`); `:38-46` (`isLockedOut`); `:49-57` (`recordFailure`); `:72-80` (429 response); `:82-83` (artificial 400ms delay) | ✅ PASS | Per-IP in-memory bucket. Successful login resets the counter (`:134`). Sliding 15-min window. Artificial 400ms delay before any check slows down online brute-force. R62 notes in-memory is acceptable for single-instance Render. |
| 18 | Inquiry spam | 5 reqs / 10 min / IP | `src/app/api/inquiries/route.ts:13-14` (`RATE_LIMIT_MAX = 5`, `RATE_LIMIT_WINDOW_MS = 10 * 60 * 1000`); `:27-41` (`isRateLimited`) | ✅ PASS | Public POST endpoint rate-limited per IP. |
| 19 | Subscribe spam | 5 reqs / 10 min / IP | `src/app/api/subscribe/route.ts:18-21` (`RATE_LIMIT = 5`, `RATE_WINDOW_MS = 10 * 60 * 1000`); `:23-32` (`rateLimited`) | ✅ PASS | Public POST endpoint rate-limited per IP. |
| 20 | Payment proof upload | 5 uploads / 30 min / token | `src/app/api/portal/[token]/paid/route.ts:57-58` (`UPLOAD_RATE_LIMIT = 5`, `UPLOAD_WINDOW_MS = 30 * 60 * 1000`); `:60-69` (`uploadRateLimited`); `:109-112` (429 response) | ✅ PASS | Per-token in-memory bucket. A compromised portal link can flood at most 5×10MB in 30min before the bucket trips. Magic-byte validation (`:39-50`) + max file size (`:106-108`) bound each upload. |
| 21 | Analytics track | 60 reqs / min / IP | `src/app/api/analytics/track/route.ts:16-17` (`RATE_LIMIT = 60`, `WINDOW_MS = 60_000`); `:20-29` (`rateLimited`); `:32-35` (opportunistic cleanup interval) | ✅ PASS | Per-IP in-memory bucket. Opportunistic cleanup via `setInterval(...).unref?.()` prevents unbounded growth. |
| 21b | AI chat | 20 msgs / min / IP | `src/lib/ai-chat.ts:292-293` (`RATE_WINDOW_MS = 60_000`, `RATE_MAX = 20`); `:295-306` (`checkRateLimit`); called from `src/app/api/ai/chat/route.ts:40` | ✅ PASS | Per-IP in-memory bucket with 5000-bucket hard cap (`ai-chat.ts:304`). |

### G. Logging of Sensitive Values

| # | Category | Item | File + Line | Verdict | Notes |
|---|---|---|---|---|---|
| 22 | Sensitive logging | No console.log of passwords, tokens, API keys, customer PII | `Grep "console\.(log\|info\|warn\|error)" src/` returns 100+ hits across all admin/lib routes — every entry is an error or operational status log (e.g. `[admin/login] ADMIN_EMAIL / ADMIN_PASSWORD env vars are not set`, `[notify:webhook] delivery failed: ${err}`, `[paystack-webhook] background processing crashed: ${evt.event}`) | ✅ PASS | Verified each log call manually: (a) `src/app/api/admin/login/route.ts:93,158` logs ONLY the env-var-not-set state + the raw `err` object (which is a Prisma/Node error, never the request body — the parsed `body` is consumed by line 102 + not referenced in the catch); (b) `src/lib/notify.ts:117` (`[crm:message] webhook delivery failed: ${err}`) — `err` is a `fetch` failure / TypeError, never the `webhookUrl` value (which is `process.env.NOTIFY_WEBHOOK_URL` — env var, not logged); (c) `src/lib/email-failover.ts:245-246` logs `${provider} failed (${result.error})` — provider name + the HTTP error code, NOT the webhookUrl or apiKey; (d) the WhatsApp routes log `err` only, never the `WHATSAPP_INTERNAL_TOKEN` value. No `console.log` of `password`, `parsed.data.password`, `adminPassword`, `apiKey`, `secretKey`, `secureToken`, or `confirmToken` anywhere in src/. |

### H. Production Error Leakage

| # | Category | Item | File + Line | Verdict | Notes |
|---|---|---|---|---|---|
| 23 | Build errors | `ignoreBuildErrors: false` (Phase 27 fix) | `next.config.ts:13` (`typescript: { ignoreBuildErrors: false }`) | ✅ PASS | Verified. TypeScript errors now block production builds. |
| 24 | Error responses | API routes catch errors + return generic messages (verified on 3 routes) | `src/app/api/inquiries/route.ts:181-187,281-287,324-336` (try/catch wraps every handler, returns generic `{error:"Failed to submit/ load/ update inquiry"}`); `src/app/api/admin/login/route.ts:157-163` (catch returns generic); `src/app/api/paystack/webhook/route.ts:30-52` (returns `{error:"webhook secret not configured"}` / `{error:"invalid signature"}` — never the secret) | ✅ PASS | Every handler has a top-level try/catch. The detailed error is logged server-side (`console.error("[ROUTE]", err)`); the client gets a stable generic message. No stack trace crosses the wire. Paystack webhook logs rejected signatures to `webhook_logs` for the audit trail (the payload + signature header — NOT the secret). |

### I. Email Link Abuse (Master Directive §7 particular focus)

| # | Category | Item | File + Line | Verdict | Notes |
|---|---|---|---|---|---|
| 25a | Email link abuse | Portal link `/portal/{secureToken}` is 192-bit unguessable, no enumeration | `src/lib/portal.ts:13-15` (32 random bytes → base64url 43-char token); `src/app/portal/[secureToken]/page.tsx:32-35` (server-side `findUnique({where:{secureToken}})`); `Grep "secureToken"` returns zero public-listing endpoints | ✅ PASS | 32 random bytes = 256-bit entropy (the directive's "192-bit" floor is conservative). No public endpoint lists tokens. No sequential IDs. No pattern (each token is `crypto.randomBytes`-fresh per invoice). The token is `@unique` in the Invoice schema, so a single token resolves to at most one invoice — no ambiguity. |
| 25b | Email link abuse | Confirm link `/api/subscribe/confirm?token={confirmToken}` is 256-bit unguessable | `src/app/api/subscribe/route.ts:34-36` (`makeToken = crypto.randomUUID().replace(/-/g,"") + crypto.randomUUID().replace(/-/g,"")` → 64-hex chars = 256 bits); `src/app/api/subscribe/confirm/route.ts:19-21` (`findUnique({where:{confirmToken}})`); production response does NOT return the confirmPath (Phase 27 fix at `subscribe/route.ts:122-134`) | ✅ PASS | Two concatenated UUIDs = 64 hex chars = 256 bits of entropy. Lookup is by `confirmToken` (NOT by email — no enumeration via email guessing). On success, the `confirmToken` is nullified (`subscribe/confirm/route.ts:33`) so it can't be replayed. The Phase 27 fix gates the `confirmPath` field to non-production + dev-simulation-only — production responses return only `{ok:true}` so an attacker who only knows an email can't auto-confirm without inbox access. |
| 25c | Email link abuse | Unsubscribe link `/api/subscribe/unsubscribe?token={unsubscribeToken}` is 256-bit unguessable | `src/app/api/subscribe/route.ts:34-36` (same `makeToken()` helper, applied to `unsubscribeToken`); `src/app/api/subscribe/unsubscribe/route.ts:20-22` (`findUnique({where:{unsubscribeToken}})`) | ✅ PASS | Same 256-bit entropy as confirmToken. Lookup is by `unsubscribeToken` (NOT by email — no enumeration). On success the `confirmToken` is nullified but `unsubscribeToken` is preserved so the user can re-load the page after unsubscribing (the page shows "already unsubscribed" on a second click — no PII leak since the email is rendered but the email was already known by anyone with the link). |
| 25d | Email link abuse | Portal link does NOT leak invoiceId in URL | `src/app/portal/[secureToken]/page.tsx:17-40` (URL path is `/portal/{secureToken}` — no invoiceId anywhere); `src/app/api/portal/[token]/*.ts` (3 routes — all use `{token}` URL param, none read invoiceId from URL or body) | ✅ PASS | The portal URL is `/portal/{secureToken}` exclusively. No invoiceId in path, query, or hash. The customer cannot guess another invoice's URL by incrementing an ID. |
| 25e | Email link abuse | Portal does NOT leak other customers' data in rendered HTML | `src/app/api/portal/[token]/route.ts:27` (`db.invoice.findUnique({where:{secureToken:token}})` — single-row lookup); `:52-89` (response shape is the single invoice's data: invoiceNumber, customerName, service, description, amountNaira, currency, durationLabel, dueDate, status, paidAt, sentAt, createdAt, portalViewedAt, dva, pdf, paymentProof, proposal — all from this ONE invoice) | ✅ PASS | The endpoint resolves exactly one invoice by secureToken. The response carries only that invoice's data. There is no batch/list path on this endpoint. A customer hitting `/portal/{theirToken}` only sees their own invoice — never another customer's. |
| 25f | Email link abuse | Cross-customer email-link attack infeasible | Combination of (25a) 256-bit secureToken + (25b) 256-bit confirmToken + (25c) 256-bit unsubscribeToken + `@unique` DB constraints + `findUnique` lookups (no `findMany` on tokens) + invalid-token 404 + Phase 27 production-gated `confirmPath` | ✅ PASS | To access another customer's invoice, an attacker must guess a 256-bit random string — infeasible at any practical attempt rate (2^128 attempts at 1B/sec ≈ 10^11 years). To confirm-as-someone-else, the attacker needs the victim's inbox (Phase 27 fix). To unsubscribe-as-someone-else, same. The email-link abuse class is closed. |

---

## C. Fixes Applied

**None.** Zero critical findings requiring fixes. The Phase 27 security lockdown (commit `629dc44` and follow-ups — Phase 27 worklog entry at `worklog.md:3309-3438`) plus the Phase 29 AES-256-GCM email-provider credential encryption (`src/lib/email-config.ts:84-135`) already closed every security gap the third-party audit surfaced.

The Master Directive §12 mandate — "no workflow changes; close only security gaps" — was respected by NOT introducing changes when no gaps exist. The audit's role is verification, and verification returned clean.

---

## D. Remaining Security Debt (acceptable-for-single-instance today; document for the future)

These items are NOT Batch 7 findings — they are pre-existing debt items documented in the B0-A matrix (worklog.md:3997-4115) and acknowledged by the founder. They are tracked here for completeness.

| # | Debt | File / Symptom | Acceptable today because… | Address when… | Disposition |
|---|---|---|---|---|---|
| R57 | History purge of customer PDFs | Commits `fddfcc3` + `a9fe579` contain 5 customer proposal PDFs (INV-2026-0001/0007/0008/0009/0010) + 1 receipt PDF + 1 payment-proof PDF, publicly accessible via `git clone` of older revisions | The PDFs were untracked from HEAD at commit `629dc44` (Phase 27 Fix 1) so they no longer ship with new clones of `main` | Founder runs `git filter-repo --path data/uploads/ --path e2e-shots/module7/receipt-INV-2026-0001.pdf --invert-paths && git push --force-with-lease origin main` as a deliberate security-incident step (Phase 27 worklog entry `worklog.md:3431-3437` has the runbook) | Founder action — out of agent scope per Master Directive §12 |
| R62 | In-memory rate limits are process-local | `src/app/api/admin/login/route.ts:27` (loginAttempts Map); `src/app/api/portal/[token]/paid/route.ts:59` (uploadBuckets Map); `src/app/api/subscribe/route.ts:19` (rateBuckets Map); `src/app/api/inquiries/route.ts:16` (rateLimitBuckets Map); `src/app/api/analytics/track/route.ts:18` (hits Map); `src/lib/ai-chat.ts:291` (rateBuckets Map) | Single-instance Render free tier today — process-local state is correct because there's only one process. Rate limits DO work for their threat model (single-IP brute-force, single-token upload flood, etc.) | The app scales to multiple Render instances (multi-region or horizontal scaling) — at that point the in-memory Maps diverge per instance and an attacker can route around the limit by load-balancing across instances | Swap the Maps for `@upstash/ratelimit` (Redis-backed) or `BullMQ` (Redis-backed durable queue). Minimal code change because the rate-limit API surface is already a `function(ip): boolean` shape |
| R68 | CRM Excel/CSV → LLM PII governance | `src/app/api/admin/customers/import/route.ts:225-262` (default path sends spreadsheet rows to `z-ai-web-dev-sdk` LLM for column mapping) | B1-B (worklog.md:4213-4257) added the `CRM_IMPORT_NO_LLM=true` opt-out flag (`import/route.ts:147-172,223-228`) — the founder can flip the flag in Render's Environment tab to disable LLM exfiltration without code changes. Deterministic header-name mapper (`:264-291`) is the fallback path. | The founder's internal PII policy / customer DPA / regulator guidance prohibits sending customer spreadsheet data to any third-party LLM | Founder sets `CRM_IMPORT_NO_LLM=true` on Render — already documented in `.env.example` (PII GOVERNANCE section) + `docs/WORKFLOWS.md` W17 |
| R69 | Background payment processing is fire-and-forget | `src/app/api/paystack/webhook/route.ts:109-126` (`void processPaystackEvent(...).catch(...)`) | Idempotent dedup on `(provider, event, paystackId)` triple via DB `@unique` (verified at `src/lib/payment-webhook.ts:136-178`) means a crash mid-processing leaves the row in `received` status — Paystack retries the webhook, the new attempt finds the in-flight row + returns `inFlight:true` (route.ts:76-79) without re-running the heavy work. The dedup constraint makes fire-and-forget safe at today's volume | Volume grows large enough that in-process async work overflows the event loop or the `received` queue builds up beyond retry budgets | Swap `void processPaystackEvent(...)` for a durable BullMQ/Redis queue (the worker would call `processPaystackEvent` with the same dedup semantics) |

**Defense-in-depth items (not in B0-A matrix, surfaced by this Batch 7 audit, mitigated by existing safeguards — no action required):**

| # | Item | File + Line | Mitigated by | Address when… |
|---|---|---|---|---|
| L1 | Admin login credential comparison uses `===` (non-timing-safe) | `src/app/api/admin/login/route.ts:121` | 5-failed-attempts-per-15-min brute-force rate limit (`login/route.ts:24-25,72-80`) + 400ms artificial delay per attempt (`:82-83`) make timing-attack oracle infeasible (an attacker can make at most 5 attempts per 15min — far below the ~10^4 measurements needed for a byte-by-byte timing attack) | The rate limit is ever weakened (e.g. raised above 50/15min). At that point swap `parsed.data.password === adminPassword` for `crypto.timingSafeEqual(Buffer.from(parsed.data.password), Buffer.from(adminPassword))` with length-check guard |
| L2 | WhatsApp `X-Internal-Token` comparison uses `!==` (non-timing-safe) | `src/app/api/whatsapp/inbound/route.ts:14`; `src/app/api/whatsapp/service-event/route.ts:21` | The token is checked against an env var (not user data) + the routes are internal-only (the WhatsApp mini-service is on the same Render private network). Network jitter masks any timing signal | The mini-service moves to a separate host / network boundary. Swap for `crypto.timingSafeEqual` |
| L3 | `/api/health/ready` returns missing env-var NAMES in error response | `src/app/api/health/ready/route.ts:53` (`error: \`missing required vars: ${missing.join(", ")}\`` — could include `"ADMIN_PASSWORD"`) | The NAMES are public in `.env.example` (lines for `ADMIN_EMAIL`, `ADMIN_PASSWORD`, `DATABASE_URL` are all visible to anyone reading the repo). The VALUES are NEVER exposed (only the names). The endpoint is a deliberate readiness diagnostic — the 503 response IS the desired behaviour when vars are missing | A regulator / pen-tester flags this as defence-in-depth. Redact in production: `error: "missing required env vars"` (no list) — but the diagnostic value of the list (ops engineer sees which var is unset) is the reason it's verbose today |
| L4 | Newsletter confirm + unsubscribe HTML pages render `${email}` without explicit HTML-escape | `src/app/api/subscribe/confirm/route.ts:121` (`<span class="email">${email}</span>`); `src/app/api/subscribe/unsubscribe/route.ts:128` (same) | The `email` field is zod-validated at insert (`subscribe/route.ts:8-16` — `z.string().trim().toLowerCase().min(3).email().max(200)`). Verified empirically that zod's `.email()` regex REJECTS `<`, `>`, `"` characters (test: `"<script>alert(1)</script>@example.com"` → FAIL). So no XSS payload can ever be persisted to the subscribers table. The render is safe-by-validator | Defence-in-depth polish — add an `esc()` helper and wrap `${email}` → `${esc(email)}`. ~6 lines total. Not needed because the validator is the binding gatekeeper |

---

## E. Batch 7 Exit Gate

Per Master Directive §9 Batch 7 acceptance criteria, the following eight gates are confirmed:

| # | Exit gate question | Verdict | Evidence |
|---|---|---|---|
| 1 | No IDOR (no way to access another customer's resource by manipulating URL/ID) | ✅ CONFIRMED | Admin routes are admin-cookie-guarded (admin sees all by design — no IDOR). Portal routes resolve by `secureToken` only (`@unique` DB constraint → single-row resolution). No batch/list portal endpoint exists. (§B items 4-6) |
| 2 | No enumeration (no way to discover tokens by guessing or sequence) | ✅ CONFIRMED | Portal token = 256-bit `crypto.randomBytes(32)` (`portal.ts:13-15`). Confirm + unsubscribe tokens = 256-bit (2× UUID concatenated, `subscribe/route.ts:34-36`). No sequential IDs anywhere. Invalid token → 404 (no "expired" / "not found" leak). (§A items 3a-3d, §I items 25a-25c) |
| 3 | No XSS (no user input rendered without escaping) | ✅ CONFIRMED | Branded email HTML uses `esc()` on every dynamic value (`email-template.ts:37-43,53,55,60,67,82,90,93,118`). Blog content via ReactMarkdown 10.x without `rehype-raw` (HTML tags escape by default). Inquiry message + customer notes rendered as React text children (auto-escape). Newsletter confirm/unsubscribe pages render DB-validated email (zod `.email()` rejects `<>` — verified empirically). (§D items 11a-11d) |
| 4 | No SQL injection (no raw SQL with user input) | ✅ CONFIRMED | Zero `$queryRaw` / `$executeRaw` calls in app code (only matches are in `src/generated/prisma/*` SDK type definitions + the parameterized `db.$queryRaw\`SELECT 1\`` in the readiness probe). All DB access is via Prisma's typed API. (§D item 12) |
| 5 | No SSRF (no server-side fetch of user-supplied URL) | ✅ CONFIRMED | Every `fetch()` URL is either an env var (`NOTIFY_WEBHOOK_URL`, `WHATSAPP_SERVICE_URL`, `SELF_PING_URL`) or a hardcoded host (`api.paystack.co`, `api.resend.com`, `send.api.mailtrap.io`, `api.maileroo.com`, `oauth2.googleapis.com`, `www.googleapis.com`). Zero request-body URL fetches. (§D item 13) |
| 6 | No secret exposure (no secrets in committed code) | ✅ CONFIRMED | Grep for `sk_live_`/`ghp_`/`npg_`/`AKIA`/`-----BEGIN` in `src/` returns 0 hits. `.env` is in `.gitignore` (`git check-ignore .env` confirms). `.env.example` uses `<your-...>` placeholders. No real secrets in any tracked file. (§E items 14a-14c) |
| 7 | No sensitive-data leak (admin responses don't expose credentials, tokens, PII to unauthorized parties) | ✅ CONFIRMED | `/api/admin/email-config` GET returns credentials REDACTED (only field names, never values — `email-config.ts:192-220`). `/api/admin/customers` returns business fields only (no secureToken, no password hash, no paystackReference). `/api/admin/invoices` returns business fields only (no secureToken, no paystackReference). Public `/api/posts` + `/api/testimonials` return PUBLISHED items only (no draft leakage). (§E items 15a-15c) |
| 8 | No production error leakage (no raw stacks to clients; build errors block production) | ✅ CONFIRMED | `next.config.ts:13` `ignoreBuildErrors:false`. Every API route has a top-level try/catch that returns generic messages. The detailed `err` is logged server-side via `console.error("[ROUTE]", err)` only — never in the response body. (§H items 23-24) |
| 9 (§7 focus) | Email links cannot be abused to access another customer's info | ✅ CONFIRMED | The 3 email-link classes (portal `/portal/{secureToken}`, confirm `/api/subscribe/confirm?token={confirmToken}`, unsubscribe `/api/subscribe/unsubscribe?token={unsubscribeToken}`) all use 256-bit crypto-random tokens, resolve via `findUnique({where:{tokenField}})` (single-row, no ambiguity), return 404 on miss (no enumeration oracle), and Phase 27 gated `confirmPath` to non-production so an attacker can't auto-confirm without inbox access. To access another customer's invoice the attacker must guess a 256-bit string — infeasible at any practical attempt rate. (§I items 25a-25f) |

**Final verdict:** Batch 7 audit complete. 43 items audited, 43 passed, 0 critical findings, 0 fixes applied (Phase 27 + Phase 29 already closed every gap). Remaining debt (R57/R62/R68/R69) is either founder-action or acceptable-for-single-instance-today and explicitly deferred in the B0-A matrix.

---

## F. Verification

- `bun run lint` → exit 0, 0 errors (verified pre-audit baseline)
- `bunx tsc --noEmit` → exit 0, 0 errors (verified pre-audit baseline)
- `bun test tests/` → 187 pass + 9 skip / 0 fail / 820 expect() calls / 313ms (verified pre-audit baseline — no code changes were made so the suite is unchanged)

No code changes → no regression surface → no re-verification needed beyond the baseline.

---

## G. Cross-references

- Phase 27 worklog entry — `worklog.md:3309-3438` (the 10 audit fixes R58-R67)
- Phase 29 worklog entry — `worklog.md:3834-3958` (AES-256-GCM email failover chain)
- B0-A requirements matrix — `worklog.md:3997-4113` (the 122-row R1-R122 matrix with security items marked)
- B2 deep-trace — `worklog.md:4464-4516` (Paystack root-cause fix verification, includes the ambiguity-safe DVA matching)
- B1-A Paystack regression test — `tests/paystack-account-isolation.test.ts` (7 scenarios, 56 assertions, runs in CI when secrets are configured)
- B1-C email-link inventory — `docs/email-link-inventory.md` (per-CTA route + auth model)
- B1-C plain-text body well-formedness — `tests/email-plaintext.test.ts` (43 tests asserting no HTML/leak/placeholder in plain-text bodies)
- B3 email-render audit — `tests/email-render.test.ts` (112 scenarios asserting CTA URLs render correctly in composed HTML)
- B4 live route verification — `worklog.md:4600-4669` (headless-browser visit of every CTA)
- B5-FIX Code.gs reconciliation — `worklog.md:4768-4875` (closed 6 integration bugs at the root cause)

---

*Audit complete. No action required from the founder beyond the previously-documented Phase 27 R57 history-purge runbook.*
