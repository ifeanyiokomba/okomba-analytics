/* Public re-export of the admin portal entry — preserves the
   existing `import { AdminPortal } from "@/components/site/admin-portal"`
   contract used in src/app/page.tsx. */
export { AdminPortal } from "./portal";
